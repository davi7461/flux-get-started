apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  namespace: linkerd
spec:
  rules:
  - host: "*.xip.io"
    http:
      paths:
      - path: mydashboard
        backend:
          serviceName: linkerd-web
          servicePort: 8084